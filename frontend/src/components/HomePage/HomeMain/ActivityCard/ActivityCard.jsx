import styles from './ActivityCard.module.css';

const ActivityCard = ({
  name,
  author,
  description,
  encouragement,
  participants,
}) => {
  return <section></section>;
};

export default ActivityCard;
